@{
    ViewData["Title"] = "Menüler";
}

<div class="text-center  mx-auto" style="width: 80%">
    <div class="d-flex justify-content-between mb-5">
        <h3>Menüler</h3>
    </div>
    <div class="d-flex flex-column justify-content-start">
        @Html.Raw(ViewBag.MenulerHTML)
    </div>
</div>
